# Système
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Backups locaux temporaires (utilise plutôt l'export JSON depuis l'app)
*.backup
*.bak
backups/

# Fichiers locaux de test
test-data.json
local.json

# Node / Playwright (tests E2E)
node_modules/
test-results/
playwright-report/
playwright/.cache/

# Outils locaux (Claude Code, snapshots de restauration)
.claude/
.restore-payload.json
.restore-snapshot.json

# PRD locaux par WI (cadrage avant implémentation, pas commit)
PRD/

# Dialogues Claude ↔ Codex par chantier (review continue, pas commit)
codex/
